{smcl}
{com}{sf}{ul off}{txt}
{com}. 
. //OFF
. sysuse auto, clear
{txt}(1978 Automobile Data)

{com}. summarize foreign 

{txt}    Variable {c |}        Obs        Mean    Std. Dev.       Min        Max
{hline 13}{c +}{hline 57}
{space 5}foreign {c |}{res}         74    .2972973    .4601885          0          1
{txt}
{com}. scalar c = "Dynamic" 
{txt}
{com}. local min = `r(min)'
{txt}
{com}. scalar max = r(max)
{txt}
{com}. matrix Mat = (`r(min)', `r(max)')
{txt}
{com}. //ON
. 
. /**/ di as txt "> " _n ///
> `"> ###"' c  " text" _n ///
> `"> "' _n ///
> `"> The 24^th^ observation in the data is {c -(}c 96{c )-}"' make[24]  "{c -(}c 96{c )-} with a {c -(}c 96{c )-}price{c -(}c 96{c )-} of " _n ///
> `"> "' price[24] `". The values of the {c -(}c 96{c )-}foreign{c -(}c 96{c )-} variable range between "' `min'  " to " _n ///
> `"> "' max  ". The same values can also be extracted from matrices, for example, " _n ///
> `"> "' Mat[1,1] `" and "' Mat[1,2]  ". "
{res}{txt}> 
> ###Dynamic text
> 
> The 24^th^ observation in the data is {c 96}Ford Fiesta{c 96} with a {c 96}price{c 96} of 
> 4389. The values of the {c 96}foreign{c 96} variable range between 0 to 
> 1. The same values can also be extracted from matrices, for example, 
> 0 and 1. 

{com}. 
. 
. 
{txt}end of do-file
{smcl}
{txt}{sf}{ul off}